import 'package:flutter/material.dart';

class AppColors {
  static final Color? primaryColor = Colors.orange[700];
  static const Color primaryBackgroundColor = Colors.white;
  static final Color? secondBackgroundColor = Colors.grey[900];
}
